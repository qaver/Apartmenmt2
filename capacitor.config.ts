import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',

  appName: 'aparmentaccounting',
  webDir: 'dist/aparmtment_1/browser'
};

export default config;
